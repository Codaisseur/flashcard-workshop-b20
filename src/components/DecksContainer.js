import React, { PureComponent } from 'react'
import { connect } from 'react-redux'

class DecksContainer extends PureComponent {
  render() {
    <div>
      <h2>Pick a Deck</h2>
    </div>
  }
}

export default DecksContainer
